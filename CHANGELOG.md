# Changelog

All notable changes to this project are documented here. The format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to
[Semantic Versioning](https://semver.org/spec/v2.0.0.html).

The version in `pyproject.toml` and the newest released heading below must always agree —
this is enforced by a regression test (`t_version_matches_changelog` in `tests/run_tests.py`).

## [Unreleased]

### Added
- **`ai-defects` gate: deeper verifier via a closed-arg port wrapper + pinned CLI (SAT-1103).** Adds `scripts/ai_defects_verify.py`, a thin **adapter** that runs a deeper AI-code verifier — a **pinned CLI adopted by command** (`$AI_DEFECTS_BIN`, pinned via CI secrets `AI_DEFECTS_PIN_VERSION` / `AI_DEFECTS_PIN_DIGEST`) — behind a fixed, closed argv (`--run-dir <dir> --diff-file <file>`; no shell, no wildcards, no `$*` passthrough) and collapses the whole outcome space to a fail-closed tri-state exit: **0 PASS / 1 FAIL / 2 BLOCKED**. A missing/empty pin, a bad digest, a missing or non-executable binary, an `incomplete: true` summary (even on exit 0), a timeout, an unreadable input, bad argv, an unset run dir, or any unknown exit all record **BLOCKED** — never PASS, never a silent skip; an empty diff records **PASS** with reason `empty-diff`. `gate.py run` gains an **opt-in, backward-compatible `--exit-map`** (`CODE=STATUS`, `*` = catch-all for unmapped nonzero) so a tri-state wrapper records BLOCKED with no change to `aggregate.py` / `panel.py` — the default (exit 0 = PASS, nonzero = FAIL) is unchanged for every existing gate. New `.github/workflows/ai-defects.yml` (workflow_dispatch) installs the pinned binary to the runner only (digest-verified; never committed, never a release asset) and runs the gate fail-closed; `scripts/check_ai_defects_public_silence.sh` (wired into CI as the `public-silence` job) fails the build if a scanner/brand string or a first-party-authorship claim reaches a public surface (README*, `references/**`, `SKILL.md`, workflow names). `references/gates.md` documents the gate in **port language only**, and the pre-existing brand string in that file is scrubbed. Tests: 20 `t_ai_defects_*` cases lock the exit taxonomy, the closed-argv allowlist, the `gate.py` status mapping, NORMAL+ plan inclusion, and the public-silence check. `panel.py` / `aggregate.py` unchanged.
- **Streamable-HTTP sessions for `ar-mcp` (E3-S2b).** The local HTTP transport implements the **legacy** Streamable-HTTP session lifecycle (MCP revisions through 2025-11-25; the stateless 2026-07-28 revision removed protocol-level sessions and `Mcp-Session-Id` — see the era-enforcement entries above). A successful `initialize` mints a cryptographically-random `Mcp-Session-Id` (via `secrets`, returned in the response header; a fresh id per handshake, so sessions rotate). A request that *presents* a session id must present a **valid, server-minted** one — a forged or already-terminated id is refused with **404**, never silently honored (anti-hijack). **GET** opens the server→client `text/event-stream` channel for a valid session (missing id → `400`, forged → `404`); **DELETE** terminates a session (**204**; reuse → `404`). The session store is **bounded with LRU eviction** (`AR_MCP_HTTP_MAX_SESSIONS`, default 128), so an `initialize` flood cannot exhaust memory; the GET/SSE stream carries a **bounded socket write timeout** so a stalled client cannot pin a server thread indefinitely. Sessions are **optional** on the legacy path — a version-less client keeps working unchanged — with `AR_MCP_HTTP_REQUIRE_SESSION` (default off) making a session mandatory on every request except the `initialize` handshake and the `server/discover` probe; it composes with, and defaults on alongside, the bearer token in E3-S2c. Still localhost-only and **not** a command-execution surface; **no authentication yet** (E3-S2c). Review hardening (Codex on #54): the `MCP-Protocol-Version` check now runs on **every** verb (GET and DELETE are validated too, not just POST — a bogus pinned version → closed **400**); concurrent GET/SSE streams are **bounded** (`AR_MCP_HTTP_MAX_STREAMS`, default 64) so a valid session cannot open unlimited thread-pinning streams (past the cap → **503**, `Retry-After: 1`); a session terminated mid-stream stops SSE output immediately (the keepalive loop re-checks validity after its wait, so a `DELETE` yields **no** further keepalive); `AR_MCP_HTTP_REQUIRE_SESSION` now **rejects** a non-blank unrecognized value (e.g. `tru`) — the transport refuses to start rather than silently failing open to off; and the docs no longer imply a stateless `2026-07-28` client can open the SSE channel — the session lifecycle (GET/SSE included) is the optional `initialize`-based path, while the modern stateless path is POST-only until server-initiated messages and auth land (E3-S2c). New env: `AR_MCP_HTTP_MAX_SESSIONS` / `AR_MCP_HTTP_MAX_STREAMS` / `AR_MCP_HTTP_REQUIRE_SESSION`.
- **Protocol-era enforcement for the `ar-mcp` HTTP transport (E3-S2b review round 2).** The transport now routes by protocol era per the MCP 2026-07-28 spec, which made the revision **stateless**: it removed protocol-level sessions and the `Mcp-Session-Id` header, removed the `initialize` handshake, and **replaced the HTTP GET stream** with a POST `subscriptions/listen` stream. So the session lifecycle (`initialize` → `Mcp-Session-Id` → GET/DELETE) is now correctly treated as **legacy** (revisions through 2025-11-25), and a **GET or DELETE pinned to a modern version (`2026-07-28`) is `405`** — the modern era has no session channel to open or terminate (CodeRabbit r3941912010). A POST that pins an `MCP-Protocol-Version` header **and** declares `params._meta.io.modelcontextprotocol/protocolVersion` must have the two **agree**, else the contradictory request is refused `400` (r3941912010). Each session is now **bound to the protocol version its `initialize` negotiated**: a GET/DELETE/POST pinned to a *different* version is not honored for it (`404`), so a legacy session cannot be driven under a modern version (Codex r3941957895). Three independent stream fixes: a GET is **revalidated after acquiring its stream slot and before the `200`** so a session terminated in that window never receives `200` + `: connected` (TOCTOU, Codex r3941957879 — **this "never" was later relaxed to best-effort in round 6 below, which moved the `200`/`: connected` commit off the dispatch lock; a DELETE landing between the `wake.is_set()` check and the `: connected` write can still emit that one comment line on this localhost-only, single-user, pre-auth transport, with strict no-output-after-terminate ordering deferred to E3-S2c; Codex r3951751944**); a GET whose `Accept` does not admit `text/event-stream` is refused **`406` before a slot is acquired** (Codex r3941957888); and terminating (or evicting/​shutting down) a session **wakes its open SSE stream immediately**, releasing the bounded stream slot at once instead of holding it until the next keepalive tick — so the `503`'s `Retry-After` is no longer a lie (Codex r3941957873).
- **Era-enforcement follow-ups (E3-S2b review round 3).** From the CodeRabbit + Codex re-review of the era-enforcement commit: a **POST that pins a modern `MCP-Protocol-Version` on a legacy `initialize` body is now `400`** (CodeRabbit r3943894656 / Codex r3943958158) — the header/`_meta` check does not fire when the body carries no `_meta`, so a modern-pinned `initialize` previously negotiated a legacy version yet echoed the modern one and minted an `Mcp-Session-Id` the client could never use (its modern GET/DELETE/POST are `405`/`405`/`404`) while still consuming an LRU slot, letting repeated modern handshakes evict live legacy sessions. The **GET stream re-validation and wake registration are now atomic** (Codex r3943958149): `register_wake` validates the session and registers under one store lock and returns a pre-set event when the session is already gone, which the handler checks before committing the `200`, closing the window in which a DELETE landing exactly at registration could still stream `: connected` for a dead session. A **client disconnect now frees the stream slot promptly** (Codex r3943958155): the keepalive wait polls on a bounded interval and detects a peer close via `select` + a `MSG_PEEK` EOF, so a client that closes right after `: connected` releases its slot within the advertised `Retry-After` instead of holding it until the next keepalive write. And the **`Accept` parser honors media-range parameters** (Codex r3943958164 / CodeRabbit r3943913914): a parameterized range such as `text/event-stream;level=1` only matches a representation carrying that parameter, so it no longer overrides a plainer acceptable alternative — `text/event-stream;level=1;q=0, text/event-stream;q=1` now correctly admits the stream instead of returning `406`.
- **Negotiated-version match (E3-S2b review round 5).** From the re-review of round 4: a legacy `initialize` whose `MCP-Protocol-Version` header disagrees with the version the handshake will **negotiate** is now `400` — the round-4 guard compared the header to the raw body `protocolVersion`, so a body that **omits** `protocolVersion` (or sends an unsupported/non-string one) slipped through: it negotiates `SUPPORTED_PROTOCOLS[0]` while the response echoes the header, leaving the client pinned to an echoed version its own `Mcp-Session-Id` then `404`s on. The check now compares the header to the negotiated version (body version when supported, else `SUPPORTED_PROTOCOLS[0]`) (CodeRabbit r3945516733).
- **Session termination stays off the dispatch lock (E3-S2b review round 6).** Rounds 4–5 progressively took `_HTTP_DISPATCH_LOCK` on `DELETE` (termination) and the `GET` SSE `200`-commit, to make termination atomic with request dispatch. But `do_POST` holds that same lock across `serve_message()`, which runs a tool for up to `AR_TIMEOUT_S` (~300 s), so a concurrent `DELETE` or `GET` could block for the whole tool call (CodeRabbit 🟠 Major r3945707197) — and the atomicity was incomplete anyway (the `: connected` write and LRU eviction fall outside the lock; Codex r3945744724 / r3945744727). Round 6 **reverts** the `DELETE`/`GET`-commit lock acquisition: `terminate()` is internally synchronized and O(1), so a `DELETE` now takes effect immediately regardless of an in-flight tool, and a `GET` opens its stream without waiting on dispatch. `do_POST` still re-checks the session under the lock immediately before dispatch (unchanged). What is now **best-effort** on this **localhost-only, single-user, pre-auth** transport is the strict "no dispatch or stream output after terminate" ordering — a multi-client / revocation property (whose full form requires cancelling an in-flight tool, not mutual exclusion) **deferred to E3-S2c** (auth + remote bind), where it is designed against the committed threat model. Two best-effort behaviors are accepted in the meantime: a `DELETE` does **not** cancel an already-admitted tool — `do_POST` re-checks the session under the lock immediately before dispatch, but a `DELETE` landing after that re-check and before `serve_message()` lets the admitted request run to completion (its `204` can return before the tool even starts) — and a `GET` racing a `DELETE` may emit one stray `: connected` for a session just terminated. Both are self-races for the single local user; strict ordering and in-flight cancellation arrive with the multi-client surface in S2c.
- **Reject a modern-declared `initialize` (E3-S2b review round 7).** An `initialize` body that declares the modern era in `params._meta` (`io.modelcontextprotocol/protocolVersion: 2026-07-28`) is now rejected as method-not-found (`-32601`) instead of being served as a legacy handshake: the stateless 2026-07-28 revision has no `initialize`, so serving one negotiated a legacy version and (over HTTP, where no `MCP-Protocol-Version` header is present to trip the era check) minted a legacy `Mcp-Session-Id` for a client that declared modern — a contradictory state on which its own modern requests then fail: a modern **GET or DELETE is `405`** (the stateless era is POST-only, with no session channel to open or terminate) and a modern **POST `404`s** on the session's negotiated-version mismatch. The era is now validated from the request **body** in the shared `handle()` core, independent of transport or header presence, so the legacy handshake requires a legacy (`_meta`-less) body (Codex r3949809506).
- **Browser CORS deferred to E3-S2c (E3-S2b review round 8).** The S2b HTTP transport's `Origin` allow-list (`AR_MCP_HTTP_ORIGINS`) is anti-rebinding **defense** (reject a disallowed origin, per the committed threat model), not browser **enablement**: it does not emit `Access-Control-Allow-Origin`, answer the CORS preflight (`OPTIONS`), or give a browser `EventSource` a way to carry the session — so an allow-listed browser origin is *permitted at the gate* but cannot yet drive the transport. Making it browser-usable — ACAO (the validated origin) on POST/GET/DELETE, an `OPTIONS` preflight, and a browser-usable session mechanism — is one coherent unit **deferred to the E3-S2c auth/session story**, designed there against the threat model rather than retrofitted here; a lone SSE `Access-Control-Allow-Origin` header would be a half-measure a real browser still can't reach (`EventSource` cannot set the required `Mcp-Session-Id` header, and a browser `POST` preflight — an `OPTIONS` request — gets `405` from this POST/GET/DELETE-only endpoint, not a valid preflight response). The standing limitation is documented in `references/config.md` (the `AR_MCP_HTTP_ORIGINS` entry); the S2c work is tracked as an `e3-s2c-intake-browser-cors` intake note in the project's engineering roadmap (a planning doc, not a file in this repo) (Codex `r3950383352` / `r3951256109`, PR #54).
- **Combine repeated `Accept` field lines + correct the round-8 CORS note (E3-S2b review round 9).** From the Codex re-review of round-8: (1) `do_GET` negotiated the SSE `Accept` from only the FIRST `Accept:` field line (`get("Accept")`), so a client (or intermediary) that split the list-valued header across lines — `Accept: application/json` then `Accept: text/event-stream` (RFC 9110 §5.3) — got a wrong `406` even though the combined value admits SSE. It now combines every line via `get_all("Accept")` before negotiating; a single line behaves exactly as before, and an absent `Accept` still means accept-anything (Codex `r3951256116`). (2) The round-8 CORS note above is corrected (Codex `r3951256109`): a browser `OPTIONS` preflight returns **`405`** from this POST/GET/DELETE-only endpoint (not `501`); the standing limitation is now actually documented in `references/config.md` (the `AR_MCP_HTTP_ORIGINS` entry); and the S2c intake note is described as a project-roadmap planning doc rather than a repo path (a repo-wide search found no such file, which the earlier wording implied). Revert-proof regression `t_mcp_http_get_combines_repeated_accept_lines` (an SSE-admitting value on a *later* `Accept` line still opens the stream, in either order; two SSE-excluding lines still `406`).
- **`Accept` equal-rank tie, strict-mode discovery, a stale no-race claim, and the browser fire-and-forget POST (E3-S2b review round 10).** From the Codex re-review of round-9:
  - **`Accept` negotiation is order-independent among equal-specificity alternatives** (Codex P3 `r3951751953`): `_accepts_event_stream` used a strict `r > best_rank`, so with equal-specificity ranges of differing quality the FIRST occurrence permanently won — `text/event-stream;q=0, text/event-stream;q=1` returned `406` while the reverse opened the stream (also reachable after combining repeated `Accept` field lines). Ties are now OR-merged (an acceptable equal-rank alternative wins), so the outcome no longer depends on field order; a MORE specific range still overrides a less specific one, so `*/*;q=1, text/event-stream;q=0` still correctly refuses. Revert-proof `t_accepts_event_stream_equal_rank_tie_is_order_independent`.
  - **`server/discover` no longer advertises the stateless modern revision in strict mode** (Codex P2 `r3951751963`): with `AR_MCP_HTTP_REQUIRE_SESSION` on, a stateless `2026-07-28` request has no session and is `400`'d, and a legacy-minted id `404`s (sessions are version-bound) — so the modern revision cannot be served, yet `server/discover` still listed it, letting a client select a version the endpoint then rejects. In strict mode the HTTP layer now filters `MODERN_PROTOCOLS` out of the discover `supportedVersions` (the transport-agnostic `_discover_result` stays era-complete for stdio and non-strict HTTP), so a client negotiates a legacy, session-bearing version instead. Exempting modern from the session requirement was declined — it would let modern requests **bypass** strict mode, defeating its purpose. Revert-proof `t_mcp_http_discover_omits_modern_in_strict_mode`.
  - **A stale round-2 no-race claim is corrected** (Codex P2 `r3951751944`): the round-2 entry above said a session terminated in the validation-to-commit window "never" receives `200` + `: connected`, but round 6 moved that commit off the dispatch lock and explicitly accepts a residual best-effort window (a DELETE between the `wake.is_set()` check and the `: connected` write can still emit that one comment line). The round-2 wording is qualified as historical and pointed at round 6's final best-effort behavior; strict no-output-after-terminate ordering stays deferred to E3-S2c.
  - **The browser fire-and-forget POST is now accurately disclosed** (Codex P2 `r3951751957`): round-8/9 said the `Origin` allow-list does not make the transport browser-usable, but an allow-listed origin can still send a `no-cors` POST with a CORS-safelisted `Content-Type: text/plain` (which `do_POST` does not reject) carrying a JSON-RPC body, and the tool **executes** (write-only — the response is opaque to the page). `references/config.md` (the `AR_MCP_HTTP_ORIGINS` entry) now discloses this exposure and warns against allow-listing an untrusted origin. Fully closing it — rejecting non-JSON `Content-Type`s so a cross-origin browser must send a **pre-flighted** `application/json` (which `OPTIONS` → `405` blocks) — is folded into the E3-S2c browser-CORS intake note rather than bolted on ahead of that coherent CORS/auth design; it also carries a shared-test-helper blast radius (every HTTP POST test would need an explicit `Content-Type`). No behavior change this round for this item — documentation + S2c intake only.
- **Reject conflicting `MCP-Protocol-Version` headers (E3-S2b review round 11).** From the Codex re-review of round-10 (Codex P2 `r3952163012`): `MCP-Protocol-Version` is a singleton control header, but a client/intermediary may split or repeat it across field lines (RFC 9110 §5.3), and `self.headers.get()` read only the FIRST — so a contradictory later value (`2025-06-18` then `1999-01-01`, or a legacy value then the modern revision) bypassed `_protocol_ok`'s version check **and** the downstream session-version binding, which also reads the first value (Codex reproduced an `initialize` carrying a smuggled second version receiving `200` + a session id). `_protocol_ok` — shared by every verb (POST/GET/DELETE) — now reads `get_all("MCP-Protocol-Version")` and **rejects `400` when the header carries more than one DISTINCT value**; identical repeats stay harmless (the first value equals the rest), so a benign duplicating intermediary is tolerated and the single-header path is unchanged. Revert-proof regression `t_mcp_http_rejects_conflicting_protocol_version_headers` (two distinct version lines, and legacy-then-modern, both `400`; identical repeats still `200`), which returns `200` on the base.

### Fixed
- **`ar-mcp` follow-up on the #7c review (PR #24, merged before its review landed; refined again after the CodeRabbit + Codex review of the follow-up PR #55).** Hardened the MCP tool handlers in `scripts/mcp_server.py`:
  - **`ar_init` returns the run it created**, parsed from the **basename of the run-dir path** in `panel.py init`'s stdout (`initialized <path>`), instead of the lexicographically-newest `run-*` directory or the first `run-…` substring in stdout — a directory scan races a concurrent init and mis-sorts `run-…-9` vs `run-…-10`, and an unanchored substring search would match a `run-YYYYMMDD-HHMMSS` segment inside `AR_RUN_DIR` or any ancestor directory. The "newest run" fallback sorts the `-N` disambiguator numerically too.
  - **Every `ar_*` tool that targets a run resolves the newest run once and passes it as an explicit `--run` to every subprocess.** `mcp_server`'s numeric run ordering (`run-…-10` > `run-…-9`) otherwise diverges from `panel.py`/`aggregate.py`'s lexicographic newest-sort, so `ar_panel_run` could write `context.md` into one run while reviewer artifacts and the verdict landed in another. Resolving once and pinning `--run` keeps every pipeline phase on a single audit record and closes the concurrent-init TOCTOU uniformly.
  - **`ar_aggregate` never surfaces a stale verdict**: it moves any pre-existing `verdict.json` **aside** before invoking `aggregate.py` and requires a recognized exit code (0/1/2) plus a **newly written** `verdict.json` — proving freshness by the new file's existence rather than an mtime bump (which a coarse-granularity filesystem can leave unchanged on a same-quantum rewrite). An aggregation that crashes without recomputing is an error (and the prior verdict is restored), not an apparently-successful pre-existing `PASS`.
  - **`ar_check_digest`** distinguishes intact (`0`) / drifted (`1`) / cannot-verify (`2` = no verdict or no attestation → tool error), instead of collapsing a missing verdict into a false `{"intact": false}`.
  - **`ar_panel_rebuttal` added** — the rebuttal round is now reachable over MCP (keyless `prepare=true` writing per-reviewer request bodies for `ar_panel_ingest phase="rebuttal"`, or a direct HTTP call), so an MCP-only host can carry a SENSITIVE/CRITICAL run with high/critical findings to a verdict instead of staying BLOCKED.
  - **`authorized_by` must be a non-empty string** across `ar_gate_plan` / `ar_gate_record` / `ar_panel_assign`: a schema-invalid value (bool/number/list) is rejected rather than stringified (e.g. `true` → `"True"`) into a named authorizer that waives a gate.
  - **`catalog_file` confinement** now also rejects Windows-absolute paths (drive letter, backslash, UNC), and — beyond the lexical checks — **resolves the path (following symlinks) and rejects any target outside the working tree**, so a relative path that is itself a symlink to an out-of-tree file cannot make the CLI read it. **`ar_panel_run` forwards `catalog_file`** so a reviewer substitution can resolve when the live catalog is unavailable.
  - **A falsy non-`dict` `arguments`** (`[]`, `""`, `0`, `false`) on `tools/call` is now a `-32602` error instead of being silently defaulted to `{}`.
  - The **panel-run wrapper timeout scales with `AR_TIMEOUT_S`** budgeted for `panel.py`'s true worst case — up to **9 request budgets per role** (four primary attempts — two outer × a corrective-JSON retry — plus one live model-catalog fetch during substitution, plus four substitute attempts) across up to 6 roles — so a legitimately slow-but-valid run is not killed before its retry/substitution protocol finishes.
  - Removed the **hardcoded model slug** from the `ar_panel_assign` tool schema (a concrete `provider/model` goes stale and steers hosts to pin it); uses a `<provider>/<model-slug>` placeholder.
  - **Durability + integrity hardening from the latest review round (CodeRabbit + Codex on PR #55, plus a dev-side Fable pass):**
    - `ar_aggregate`'s prior-verdict restore is now **crash-durable**: when `verdict.json` cannot be moved aside to `.prev`, the prior is snapshotted to a durable `verdict.json.bak` **before** aggregation (an in-memory copy alone was lost if the process died mid-run or the write-back itself failed), and a restore failure that occurs **while another error is unwinding** (e.g. a subprocess timeout) is now folded into that error instead of silently dropped — so a client is never told only "aggregate timed out" while its accepted verdict sits stranded and `ar_get_verdict` can no longer return it.
    - `ar_check_digest` treats a **missing or typo'd run** as a tool error, not a false `{"intact": false}` (a nonexistent run makes `aggregate.py` exit 1 — the same code as real drift); and a **pathologically deep `verdict.json`**, whose parse raises `RecursionError` (a `RuntimeError` subclass, not `ValueError`), is cannot-verify (exit 2), never a crash the wrapper reads as drift. `aggregate.py`'s attestation over recorded artifacts likewise raw-hashes a `RecursionError`-deep artifact instead of crashing aggregation.
    - `mcp_server` puts its own directory on `sys.path` (as `panel.py` / `aggregate.py` do) so the in-process `catalog_file` validation and policy `high_samples` timeout budgeting work under the `ar-mcp` console-script entry point too — not only when the module is launched by file path.
    - Run-directory selection anchors `RUN_RE` with `\Z` instead of `$` (which also matches just before a trailing newline): a crafted `run-…\n` directory in the untrusted repository tree can no longer pass `RUN_RE.match`, sort "newest", and redirect every run-pinned tool to that non-minted directory. And `ar_panel_run` rejects a `catalog_file` that is not a regular file before loading it, so a FIFO in the working tree can no longer block the in-process catalog load — which no `_run_cli` timeout guards — and hang the server.
    - Lower-severity follow-ups from the dev-side pass: a run killed between a move-aside and its settle leaves the prior at `verdict.json.prev`, which the next `ar_aggregate` adopts and restores; a rejected aggregate now names *why* (stale / malformed / unrecognized / wrong-run); and `authorized_by` is trimmed of surrounding whitespace before it enters the audit record.
    - Corrections from the CodeRabbit + Codex review of that round (commit `9b93b4c`): `ar_aggregate` **refuses to aggregate unpinned** — with no run passed and none yet minted it errors (`call ar_init first`) rather than resolving the newest run itself, since an unpinned resolve lets a run a concurrent caller inits in the meantime be aggregated (and its `verdict.json` mutated) by a call that never selected it; every aggregate now carries an explicit `--run`, which subsumes fix-15's accept-time run-dir pin (that fixed only *which* verdict got surfaced, not the concurrent mutation — CodeRabbit's merge-risk). A successful `ar_init` whose stdout can't be parsed for the run id now **returns a tool error** (the new run is on disk under the run root) instead of falling back to a newest-run directory scan that re-opened the concurrent-scan race and could hand back a valid-looking *wrong* id.
    - Corrections from the CodeRabbit + Codex review of that round (commit `274b460`): a recovery sidecar (`verdict.json.prev`/`.bak`) already present next to `verdict.json` is now **refused**, not silently reconciled. Such a sidecar is ambiguous — it is *either* a crash mid-settle (the sidecar is the last accepted verdict and `verdict.json` the crashed output) *or* a **successful** aggregate whose best-effort sidecar cleanup failed (`verdict.json` is the newer accepted verdict and the sidecar is obsolete) — and disk state cannot tell the two apart. fix-16's preserve-and-track guessed "crash" and, in the second case, rolled a newer accepted verdict **back** to an older one on a later rejection (a newer FAIL reverted to an older PASS); the rename fallback likewise overwrote an existing good `.bak`, destroying the last accepted verdict (both Codex). `ar_aggregate` now refuses at entry when a sidecar **file** is present — leaving both files for the operator to reconcile so nothing is rolled back or lost — while the unambiguous crash-recovery path (`verdict.json` absent, a stranded `.prev`) still auto-recovers, and a stray `.prev` *directory* still falls through to the move-aside fallback. Two recovery error messages were also sharpened (CodeRabbit): `ar_init`'s unparseable-stdout hint now names the actual run root (`AR_RUN_DIR` when set, not a hardcoded `.adversarial-review/`) so it never points at an empty directory, and the snapshot-failure error now includes the underlying `OSError` cause (the `tools/call` path sends only the message text to clients).
    - Correction from the Codex review of that round (commit `8c999b9`): `aggregate.py`'s attestation is now **byte-identical across Python versions**. It hashes each recorded artifact by canonicalizing it (`json.loads` → sorted, compact JSON) and falls back to a raw-byte hash when that fails — but whether a deeply nested artifact *failed* was decided by `json.loads` raising `RecursionError`, a threshold that differs by Python version, so the same 20,000-level artifact hashed canonically on one interpreter and raw on another and a verdict made under one version reported false `DRIFT modified` when checked under another (Codex reproduced this across 3.13 and 3.14). The canonical-vs-raw choice is now made from a **fixed, version-independent nesting-depth cap** measured from the bytes *before* parsing, so an artifact takes the same path — and yields the same digest — on every interpreter; real artifacts (a handful of levels deep) are canonicalized exactly as before.
    - Corrections from the CodeRabbit + Codex review of that round (commit `5833535`), two follow-ons from the depth-cap change:
      - **Legacy attestation compatibility** (CodeRabbit 🟠 Major `r3924074998` / Codex P2 `r3924189603`): a verdict recorded *before* the cap canonicalized a deep-but-parseable artifact and stored a plain canonical hash, which this version now hashes `raw:`, so `--check-digest` recomputed a different manifest for **unchanged** bytes — and because the algorithm id stayed `sha256-canonical-json-v1`, the checker could not date the verdict and reported false `DRIFT` (exit 1) right after an upgrade. When *every* differing file is that representation change **and** the current bytes provably re-canonicalize to the stored canonical hash (positive proof the content is unchanged, not tampered), `--check-digest` now reports **cannot-verify (exit 2)** with re-aggregation guidance (`LEGACY <file>`) instead. Because that is a byte-equality proof, a deep artifact swapped for *different* content re-canonicalizes to a different hash and still reports `DRIFT` — the compat path never masks tampering.
      - **Per-run interprocess aggregate lock** (Codex P2 `r3924189590`): two independently launched `ar-mcp` processes aggregating the same run were serialized only by the process-wide HTTP dispatch lock, so both could move the prior verdict to the one shared `verdict.json.prev` and race the settle — Codex reproduced a sequence in which the second process's `finally` unlinked the first's newly accepted `verdict.json` with no stash left, leaving neither `verdict.json` nor `.prev`. `ar_aggregate` now takes an `O_EXCL` `verdict.json.lock` (not a `*.json` file, so it is never attested) held across the whole move-aside → aggregate → settle critical section; a second concurrent caller **refuses** (`another ar_aggregate is in progress`) instead of adopting the first's sidecar, and a completed aggregate releases the lock (close-before-unlink, so removal works on Windows too).
    - Corrections from the CodeRabbit + Codex review of that round (commit `bd827ba`) — the attestation's cross-runtime determinism is now byte-based end to end, and the legacy-compat check no longer re-parses:
      - **Attestation determinism now covers integer width, not only depth** (Codex P2 `r3930239161`): whether `json.loads` accepts a very wide integer literal depends on the runtime's integer-string-conversion limit (`PYTHONINTMAXSTRDIGITS`), a per-interpreter *config* — so canonicalizing it made the digest config-dependent and an unchanged run reported false `DRIFT` when moved between configs (Codex reproduced a 5,000-digit-integer artifact hashing canonically vs `raw:` across `PYTHONINTMAXSTRDIGITS` settings). `compute_attestation` now routes any artifact whose integer-digit run exceeds a fixed byte cap to the raw hash **before** parsing, exactly as it already did for nesting depth, so the raw-vs-canonical choice is measured from the bytes and is identical on every runtime and configuration.
      - **Legacy-compat is now version-gated, not runtime-dependent** (Codex P2 `r3930239157` / CodeRabbit 🟡 Minor `r3930172612`): fix-19's compat path re-parsed the artifact to *prove* byte-equality, but canonicalizing a deep artifact raises `RecursionError` on a lower-limit runtime, so a genuinely-unchanged deep legacy verdict still reported false `DRIFT` when checked on a smaller runtime (Codex reproduced 3.14 → 3.13). The attestation `algorithm` id is now bumped to **`sha256-canonical-json-v2`** (the id is metadata, not folded into the digest, so unchanged shallow runs still verify identically), and `--check-digest` classifies a legacy transition purely from that id being older than the current one **plus** every differing file being a canonical→`raw:` transition — never by re-parsing. A **current**-algorithm verdict is never excused (a canonical→`raw:` mismatch on it stays `DRIFT`), so the transitional leniency cannot mask tampering on a freshly aggregated run. This also corrects the wording (CodeRabbit): the check proves a version transition, not byte identity, and `scripts/aggregate.py`, `references/schemas.md`, and `README.md` now describe the guarantee accurately.
    - Corrections from the CodeRabbit + Codex review of that round (commit `3eb5a4c`) — the sidecar-recovery paths reject symlinks, the digest checker validates the stored algorithm id, and the legacy-transition wording is corrected from "unchanged" to "unverifiable":
      - **Sidecar recovery in `ar_aggregate` no longer follows symlinks** (Codex P1 `r3930666147` and `r3930666146`): both the entry guard that refuses an already-present recovery sidecar and the crash-recovery path that adopts a stranded `verdict.json.prev` used `is_file()`, which *follows* symlinks — so a `.prev`/`.bak` that is a symlink into an untrusted run directory would be adopted and then moved to `verdict.json`, after which `ar_get_verdict` follows it and returns an arbitrary external file's contents; and a **dangling** symlink, which `is_file()` reports as absent, would be written *through* by the snapshot below, creating an attacker-chosen out-of-run file. The entry guard now also trips on `is_symlink()` (an `lstat`, which does not follow and so catches the dangling case) and refuses with a message noting a symlinked sidecar is never a valid recovery file; the adopt path takes the stranded `.prev` only when it is a regular, non-symlink file and otherwise leaves it untouched and computes a fresh verdict.
      - **`--check-digest` validates the stored `algorithm` id before any legacy handling** (CodeRabbit 🟡 Minor `r3930631485`): only the current `sha256-canonical-json-v2` and recognized predecessors (`sha256-canonical-json-v1`) are interpretable. A verdict carrying a **newer, unknown, or malformed** id is no longer run through the legacy canonical→`raw:` classifier (nor reported as `DRIFT`) — this version cannot interpret that representation, so a digest mismatch is **cannot-verify (exit 2)** with a message naming the unrecognized id and the ids this version does recognize.
      - **The legacy-transition path is documented as *unverifiable*, not *proven-unchanged*** (Codex P2 `r3930666148` / CodeRabbit 🟡 Minor `r3930631493`): fix-20's `LEGACY` message and the `aggregate.py` / `schemas.md` / `README.md` prose said a digest match is impossible "even though the run is unchanged," overstating what the recorded hashes establish — from a stored canonical hash and a recomputed `raw:` hash alone the tool **cannot** distinguish a benign representation change from a real modification that kept the artifact beyond the cap (a deep artifact swapped for *different* deep content is still canonical→`raw:`). The wording now says the transition is unverifiable and re-aggregation is required. The classification is unchanged (recognized-predecessor id **plus** every differing file a canonical→`raw:` transition → exit 2, a tool error that is **never** an intact pass, so nothing tampered is let through). Reporting `DRIFT` here instead — the alternative Codex raised — is declined because it re-introduces the false alarm on an *unchanged* legacy artifact that fix-19 was created to remove, and a runtime-independent re-canonicalization of a deep artifact (which would settle the question) does not exist; a **current**-algorithm verdict is still never routed here (its canonical→`raw:` mismatch stays real `DRIFT`).
    - Corrections from the CodeRabbit + Codex review of that round (commit `7460ea1`) — the durable `.bak` fallback is recoverable when `verdict.json` is absent, the digest checker validates the algorithm id even on a matching digest, and a post-write aggregate crash can no longer masquerade as a completed verdict:
      - **`ar_aggregate` recovers (or refuses) a stranded `verdict.json.bak` when `verdict.json` is absent** (CodeRabbit 🟠 Major `r3941598640`): the absent-verdict branch recovered only a stranded `.prev`, ignoring the durable `.bak` snapshot the rename-fallback path writes. A crash that left the last accepted verdict only in `.bak` was therefore neither restored (a rejected retry lost it) nor cleaned up (a successful retry stranded the `.bak`, so the *next* call refused at the entry guard over an unrecoverable sidecar). The branch now adopts exactly one regular, non-symlink sidecar (`.prev` or `.bak`) as the recovery stash — restored on a rejected aggregate, discarded once a fresh verdict supersedes it — and refuses when a sidecar is a **symlink** (an arbitrary read/write vector, per fix-21) or when **both** a `.prev` and a `.bak` are present (ambiguous which is authoritative), mirroring the entry guard. The fix-21 symlinked-`.prev` test is tightened to assert the branch now *refuses* rather than silently ignores.
      - **`--check-digest` validates the algorithm id BEFORE the digest-equality check** (Codex P2 `r3941637877`): fix-21's algorithm-id whitelist ran only in the mismatch branch, so a verdict whose `attestation.algorithm` was changed to an unrecognized value while its digest still equalled the recompute (only the id changed) exited 0 (`attestation OK`) — reporting a representation this version cannot interpret as intact. The whitelist now runs ahead of the equality check, so an unrecognized/malformed id is cannot-verify (exit 2) **whether or not the digest matches**.
      - **`ar_aggregate` requires the aggregate exit code to match the written verdict** (Codex P2 `r3941637886`): `aggregate.py` maps its verdict to its exit code (PASS=0, FAIL=1, BLOCKED=2) and writes `verdict.json` before the human-readable `verdict.md`; a crash *after* that write (e.g. an untrusted run has `verdict.md` as a directory, so the markdown write raises) exited nonzero with a fresh, well-formed PASS `verdict.json` on disk, which the wrapper accepted as `isError: false` PASS carrying a traceback. Acceptance now also requires `rc == {PASS:0, FAIL:1, BLOCKED:2}[verdict]`, so a post-write crash is rejected as an incomplete aggregation.
    - Corrections from the CodeRabbit + Codex review of that round (commit `3ede471`) — a post-write aggregate crash now exits outside the verdict set, a FIFO backup sidecar can no longer hang the server, and the documented panel-run budget is corrected:
      - **A post-write crash in `aggregate.py` now exits 3, not 1** (CodeRabbit 🟠 Major `r3941710394`): the fix-22 exit-code/verdict match above is necessary but not sufficient — `aggregate.py` writes `verdict.json` before `verdict.md`, and on an uncaught exception Python exits **1**, identical to a real `FAIL`, so a crash that wrote a `FAIL` `verdict.json` and then died writing `verdict.md` (e.g. an untrusted run has `verdict.md` as a directory) still matched `verdict_exit["FAIL"] == 1` and was accepted. The CLI entry now maps any uncaught exception to **exit 3** — a code outside the verdict set `{0,1,2}` — so `ar_aggregate` rejects a post-write crash rather than surfacing it as a completed `FAIL`; intentional `sys.exit(...)` codes (including a subcommand's own exit 3) pass through unchanged. `references/schemas.md` documents the exit contract.
      - **`ar_aggregate` rejects a non-regular backup sidecar before writing it** (Codex `r3941758239`): when `verdict.json.prev` is a directory (so the move-aside rename fails) and `verdict.json.bak` is a FIFO, the rename-fallback's `write_bytes()` blocked forever waiting for a reader, before any `_run_cli` timeout applied (reproduced as `h_aggregate` hanging until an outer timeout). The fallback now `lstat()`s the backup path and refuses a **non-regular** sidecar (FIFO/socket/device/directory) instead of opening it.
      - **The panel-run request budget in this changelog is corrected to nine** (Codex `r3941758243`): after the catalog-reload timeout fix, `_panel_timeout()` budgets **nine** `AR_TIMEOUT_S` request intervals per role — four primary attempts (two outer × a corrective-JSON retry), one live model-catalog fetch during substitution, and four substitute attempts — so the release note above no longer under-counts the worst case at eight.
    - Corrections from the CodeRabbit + Codex review of that round (commit `2d8cfe3`) — the backup sidecar is created atomically, a stranded recovery sidecar is validated before it can be promoted, the crash exit-3 mapping covers the installed console entry point, and the rebuttal tool metadata is corrected:
      - **`ar_aggregate` creates the backup sidecar atomically** (CodeRabbit 🟠 Major `r3941991607`): the fix-23 `lstat()`-then-`write_bytes()` was TOCTOU — a symlink swapped in at `verdict.json.bak` after the check would be followed by the write and overwrite its target. The snapshot is now written through an `os.open(..., O_CREAT|O_EXCL|O_WRONLY)` descriptor, which fails (→ `ToolError`) if *anything* already exists at the path (regular file, symlink, FIFO, socket, or directory), so a pre-existing or raced sidecar can be neither followed nor blocked on.
      - **`ar_aggregate` validates a stranded recovery sidecar before adopting it** (Codex `r3942035547`): when `verdict.json` is absent, a stranded `.prev`/`.bak` was adopted as the recovery stash and, on a rejected retry, promoted to `verdict.json` — so an attacker who dropped a crafted `PASS` in the untrusted run dir could have it returned by `ar_get_verdict`. The sidecar is now adopted only if it parses as a verdict whose `run_id` matches the run **and** whose stored attestation digest re-verifies against the run's recorded artifacts (via `compute_attestation`); an unverifiable sidecar is refused and surfaced, never promoted.
      - **The post-write-crash exit-3 mapping now covers the console entry point** (Codex `r3942035551`): fix-23 placed the uncaught-exception → exit-3 wrapper in `aggregate.py`'s `__main__` block, which the installed `ar-aggregate` console script (`adversarial_review.aggregate:main`) bypasses by calling `main()` directly — so a post-write crash still exited 1 (indistinguishable from `FAIL`) for installed CLI consumers. The wrapper moved into `main()`, so both `python aggregate.py` and the console entry map an unexpected error to exit 3.
      - **The `ar_panel_rebuttal` tool description is corrected** (Codex `r3942035552`): it said rebuttal is required only for SENSITIVE/CRITICAL runs, but a NORMAL run under `rebuttal_policy=any` with high/critical findings also requires it — an MCP host relying on the metadata would skip the round and hit an avoidable BLOCKED. The description now frames the requirement by policy (`critical` / `contention` / `any`, the last explicitly including NORMAL).
    - Corrections from the CodeRabbit + Codex review of that round (commit `e67c330`) — crash recovery is now fail-closed, and freshness no longer depends on mtime:
      - **A stranded recovery sidecar is never auto-promoted** (CodeRabbit 🟠 Major `r3942141283` / Codex `r3942166700`): fix-24 adopted a stranded `verdict.json.prev`/`.bak` when its stored attestation *digest* re-verified against the run's artifacts — but `compute_attestation()` hashes the run's **public `*.json` artifacts, all attacker-writable**, and the digest never binds the verdict **value**. An attacker who writes the run dir recomputes the correct digest and flips the verdict to `PASS`; that forged sidecar passed fix-24's check and, on a rejected retry, the settle promoted it to `verdict.json` for `ar_get_verdict` to return. Recovery is now **fail-closed**: when `verdict.json` is absent, `ar_aggregate` only recomputes a **fresh** verdict and **never** promotes a sidecar — a rejected/crashed retry leaves `verdict.json` absent (RECOVERY_PENDING) and the sidecar intact, and a successful one supersedes it. `ar_get_verdict` reports RECOVERY_PENDING rather than returning the un-vetted sidecar. Promotion of a stranded sidecar moves to a dedicated, gated `ar_recover` tool (a signature over the **complete** verdict verified against an out-of-run-dir trust anchor, or an explicit operator confirmation bound to the sidecar bytes) — landing next; the now-defeated digest-only validator is removed.
      - **Rename-fallback freshness is mtime-independent** (Codex `r3942166702`): when the aside-move to `.prev` fails and the prior is snapshotted to a durable `.bak`, fix-24 proved a fresh verdict by comparing `st_mtime_ns`. A coarse-granularity filesystem can leave the mtime **unchanged** on a same-quantum rewrite, so a genuinely fresh verdict was mislabeled stale and rolled back to `.bak`. The fallback now **removes the snapshotted original**, so `aggregate` must **create** a new `verdict.json` and freshness is proven by existence (exactly as the move-aside path already does). If that unlink itself fails, aggregation now **aborts fail-closed** (see the fix-29 entry below) rather than falling back to an mtime check.
    - Corrections from the CodeRabbit + Codex re-review (commit `561122f`):
      - **A rejected verdict that cannot be removed is no longer readable as an accepted one** (Codex `r3944027644`): when no prior verdict existed and `aggregate` wrote `verdict.json` but was then rejected (e.g. an exit-3 post-write crash), a cleanup unlink that hit a transient `OSError`/Windows lock was silently ignored, leaving the rejected `verdict.json` on disk for a later `ar_get_verdict` to return as an accepted verdict. The unlink failure is no longer swallowed: the rejected file is **moved aside to a non-verdict, non-sidecar name** (`verdict.json.rejected` — not `*.json`, so never attested; not `.prev`/`.bak`, so never a recovery candidate) so it can never be read back as a verdict, and only if that move *also* fails is the failure surfaced (with guidance to remove the file).
      - **`ar_panel_rebuttal` validates `prepare` is a real boolean** (Codex `r3894216938`): the server does not auto-validate tool arguments against the advertised schema, so a truthy non-boolean (e.g. the string `"false"`) was treated as enabled and took the keyless `--prepare` path instead of running the direct rebuttal. A non-boolean `prepare` is now rejected before the transport is chosen. (Two other items in the same re-review were verified already-fixed and were not re-changed: non-object `verdict.json` in `--check-digest` is classified cannot-verify/exit 2, and the `ar-mcp` in-process helper imports resolve under the console entry point via the `sys.path` insertion — confirmed by importing the package as `adversarial_review.mcp_server`.)
      - **The rejected-verdict cleanup no longer masks the original error** (CodeRabbit `r3945458785`): the reconcile-failure log for a rejected output that could be neither removed nor moved aside referenced `where`, a variable only the *prior-restore* branch assigns — so if a **non-`ToolError`** was already unwinding, it raised an `UnboundLocalError` that masked the original exception (and the cleanup diagnostic). It now logs the already-built `detail` message, which is set for both branches.
    - Corrections from the CodeRabbit + Codex re-review (commit `e51160c`):
      - **`--sign` and `--verify-signature` require the recorded attestation's algorithm to be current** (Codex P2 `r3945556742`): both paths recompute the attestation under the current `sha256-canonical-json-v2` and then compared only the recomputed *digest* to the stored one — never the stored `algorithm` id — so a verdict whose `attestation.algorithm` was relabeled to a legacy or unrecognized/forged value while its digest still equalled the current recompute was signed (exit 0) and its signature verified (exit 0) as if current, vouching for a representation this version never actually re-attested. `--sign` now refuses such a record (exit 1, the "re-aggregate under the current algorithm before signing" refusal family — never exit 3, which is reserved for a missing/failed signer, leaving the sidecar untouched) and `--verify-signature` reports cannot-verify (exit 2, never exit 1 "not verified", which the contract reads as a detected tamper), gating the algorithm id **before** the digest comparison — mirroring the check `--check-digest` already performs ahead of its digest-equality. Signing/verifying now only vouches for an attestation produced under the algorithm this version actually recomputes under.
    - Corrections from the CodeRabbit + Codex re-review (commit `26cdd40`):
      - **`ar_aggregate` fails closed when the prior verdict can be neither moved aside nor removed** (Codex P2 `r3945727346`): in the rename-fallback path (the `.prev` move failed, so the prior is snapshotted to `.bak` and the original is unlinked to prove freshness by existence), an unlink that *also* failed — e.g. a Windows handle that shares writes but not deletes — was swallowed, leaving `before_mtime` set so freshness fell back to the timestamp check. On a coarse-granularity filesystem `aggregate` can rewrite `verdict.json` within the prior's mtime quantum, so a genuinely fresh verdict was mislabeled stale and the settle rolled a fresh `FAIL` back to the prior `PASS` (Codex reproduced). The unlink failure now **aborts before `aggregate` runs** (`ToolError`, the same fail-closed stance as fix-25's read/backup guards), leaving `verdict.json` intact and best-effort removing the redundant `.bak` — the unsafe mtime fallback is gone, so a fresh verdict is never rolled back to the prior one.
    - Corrections from the CodeRabbit + Codex re-review (commit `947241e`):
      - **Catalog validation now binds to the resolved path every consumer opens** (CodeRabbit 🟠 Major `r3950286015`): `_add_confined_catalog` resolved `catalog_file` (following symlinks) and rejected a target outside the working tree, but then forwarded the caller's **mutable relative** path to `panel.py` and re-derived the path for the in-process loadable and context-alias checks — a check-to-open (TOCTOU) gap in which an in-tree symlink swapped to an out-of-tree target **after** validation would be followed when a consumer re-resolved that relative path. `_add_confined_catalog` now **returns the validated canonical (resolved, symlink-free) `Path` and forwards that exact path** to `panel.py`, and both `_require_loadable_catalog` (size/regular-file/loadability) and `_reject_context_alias` (the run's `context.md`) take that canonical path — so the in-process loader and the `panel.py` subprocess open the one already-checked file, and a post-validation symlink swap can no longer redirect any of them (`panel.py`'s `load_catalog` accepts the absolute path unchanged). A revert-proof regression validates an in-tree symlink, swaps it to an external target after validation, and asserts the returned/forwarded path still names the original in-tree file (absent-symbol behavior on `947241e`).
    - Corrections from the CodeRabbit + Codex re-review (commit `5a1030c`):
      - **The validated catalog is now copied to a private server-owned snapshot, and every consumer reads that snapshot** (CodeRabbit 🟠 Major `r3950684588` / Codex P2 `r3950590290`): fix-31 forwarded the *canonical resolved path*, but every consumer still reopened it BY PATH — the in-process loadable check, `panel.py` at assignment, and `panel.py`'s lazy substitution reload — so a symlink swapped at that path **after** validation could still redirect a later open outside the tree (or to a FIFO), and the `context.md`-alias check only covered ONE of the run's deterministic destinations (`sample_policy.json`, written before the substitution reload, is another). `_snapshot_confined_catalog` now opens the validated path ONCE through a race-safe descriptor — `O_NOFOLLOW` turns a final-component symlink swapped in after resolution into an error instead of an out-of-tree read, `O_NONBLOCK` keeps the open from blocking on a FIFO, and `fstat` on the descriptor itself (never a separate `stat`) proves a regular file and bounds the size with no TOCTOU — and copies its bytes to a private, server-owned snapshot **outside** the repo/run tree. `ar_panel_run` and `ar_panel_assign` forward THAT snapshot and remove it in a `finally` once the panel subprocess (which reads it synchronously) has returned. Because every consumer now reads the snapshot, no by-path reopen remains to race and the catalog is decoupled from every run-written file — so the separate `context.md`-alias guard (`_reject_context_alias`) is retired, superseded by the snapshot. Two revert-proof regressions cover the decoupling (mutating then deleting the source leaves the snapshot intact) and the post-validation symlink swap; both are absent-symbol on `5a1030c`. This is hardening for a repository whose catalog content is untrusted; the transport itself remains localhost-only, single-user, and pre-auth (E3-S2c).
    - Corrections from the Codex re-review (commit `31f9316`):
      - **The in-process policy read for timeout budgeting is now size-bounded** (Codex P2 `r3950830841`): when `AR_HIGH_SAMPLES` is unset, `_panel_timeout()` resolved the corroboration sample count from the repo policy via an in-process `load_policy()`, whose `read_text()` pulls the whole `.adversarial-review.yml`/`.json` into the MCP process — a path no `_run_cli` timeout guards, so an untrusted repo's oversized or sparse policy could stall or exhaust the server merely by invoking `ar_panel_run` (the same class as the catalog in-process read, applied to the sibling policy file). `_resolved_high_samples()` now `stat`-checks the policy first (`_policy_read_bounded`): a **non-regular** (FIFO/device) or **oversized** (> `_POLICY_MAX_BYTES`, 1 MiB) policy is **not read in-process** — it budgets the clamp **maximum** instead (so a legitimately slow run is still never under-budgeted), leaving `panel.py` to read and validate the real policy in its own bounded subprocess. A real policy is a few lines, so normal runs read it exactly as before and the accurate env>policy>default budget is unchanged. Revert-proof regression `t_mcp_panel_timeout_bounds_oversized_policy_read`, absent-symbol on `31f9316`.
    - Corrections from the CodeRabbit re-review (commit `e216b72`):
      - **The in-process policy read is now race-safe, not stat-then-reopen** (CodeRabbit 🟠 Major `r3951172615`): fix-33's `_policy_read_bounded` used `Path.stat()` (which **follows symlinks**) to pre-check the policy and then let `load_policy()` reopen the same pathname — reintroducing the very check-to-open (TOCTOU) race the catalog snapshot had closed. A repository writer could swap the policy's symlink leaf to a FIFO after the `stat` (blocking the in-process read, which no `_run_cli` timeout guards) or to an out-of-tree target. `_read_policy_racesafe` now opens the policy **once** through a race-safe descriptor — `O_NOFOLLOW` rejects a symlink leaf (ELOOP), `O_NONBLOCK` stops a FIFO from blocking the open, and `fstat` on the descriptor proves a regular file within `_POLICY_MAX_BYTES` with no TOCTOU — copies its bytes to a private server-owned snapshot, and hands `load_policy` **that snapshot**, never the mutable path (the same race-safe read the catalog uses). An unsafe/oversized/malformed policy budgets the clamp maximum (never under-counts); a small regular policy is read and its `high_samples` honored exactly as before. Revert-proof regression `t_mcp_panel_timeout_reads_policy_racesafe` (oversized / symlink / FIFO refused → max budget; small regular read normally), which fails on `e216b72` where the `stat`-based check follows the symlink.
    - Corrections from the CodeRabbit re-review (commit `ae9a73a`), two gaps in fix-34's race-safe policy read:
      - **A conflicting pair of policy files is now refused** (CodeRabbit 🟡 Minor `r3951335743`): `_read_policy_racesafe` collected both `.adversarial-review.yml` and `.adversarial-review.json` but snapshotted only the first, bypassing `load_policy`'s required rejection when both exist — so a conflicting pair produced a budget from whichever was picked first instead of the conservative fallback. It now raises when `len(present) > 1` (→ the caller's clamp-max budget), mirroring `load_policy`.
      - **The policy open now fails closed where `O_NOFOLLOW` is unavailable** (CodeRabbit 🟠 Major `r3951335750`): the policy path is opened **un-resolved**, so `O_NOFOLLOW` is what stops a symlinked `.adversarial-review.yml` from being followed to an out-of-tree target — but on a platform without it (Windows) `getattr(os, "O_NOFOLLOW", 0)` silently dropped that protection and the open followed the link. `_read_policy_racesafe` now **raises when `os.O_NOFOLLOW` is absent** (→ the caller's conservative budget) rather than substituting `0`. (The catalog snapshot keeps the `getattr` fallback because it opens an already-**resolved**, symlink-free path, so `O_NOFOLLOW` is only defense-in-depth there, not load-bearing.) Both gaps are covered by new cases in `t_mcp_panel_timeout_reads_policy_racesafe` (both-files-present and `O_NOFOLLOW`-removed both budget the max), absent on `ae9a73a`.
    - Correction from the CodeRabbit re-review (commit `08d56a3`):
      - **A dangling policy symlink is no longer read as "absent"** (CodeRabbit 🟡 Minor `r3951475453`): fix-35's presence probe listed the policy files with `os.path.exists()`, which **follows** symlinks — so a **dangling** `.adversarial-review.yml`/`.json` symlink (a present link whose target is missing) was reported absent and dropped **before** the fail-closed `O_NOFOLLOW` open could reject it, and `_resolved_high_samples()` fell through to the non-conservative default `"1"` instead of the clamp maximum a present-but-unsafe policy warrants. The probe now uses `os.path.lexists()`, which tests the link itself, so a dangling (or any) policy symlink is kept and the `O_NOFOLLOW` open then fails closed on it → the conservative max budget; a regular policy, a live symlink, and a genuinely-absent policy all behave exactly as before. Revert-proof: a new dangling-symlink case in `t_mcp_panel_timeout_reads_policy_racesafe` budgets the max, and fails on `08d56a3` where `exists()` reports the dangling link absent and the budget collapses to `"1"`.
    - Correction from the Codex re-review (commit `c87411f`):
      - **The direct `ar-aggregate` CLI now honors the per-run aggregate lock** (Codex P2 `r3951566976`): fix-18's `verdict.json.lock` serialized only two **MCP** `ar_aggregate` callers — it is taken in `mcp_server.h_aggregate`, so the supported standalone `ar-aggregate`/`aggregate.py` entry point never observed it. Codex reproduced the MCP path moving an old `PASS` to `.prev`, a concurrent **direct CLI** computing and writing `BLOCKED`, the MCP child then exiting 3, and the MCP settle deleting the CLI's fresh `BLOCKED` and **restoring the stale `PASS`** — a silently rolled-back verdict, the one thing an audit tool must never do. `aggregate.py` now acquires the **same** `run/verdict.json.lock` (`O_EXCL`) around its `verdict.json`/`verdict.md` writes and **refuses (exit 3)** when it is held, so a direct aggregate that races a concurrent MCP aggregate backs off instead of writing into (and being rolled back by) the MCP critical section. To avoid the MCP path deadlocking against its **own** child — `h_aggregate` already holds the lock across its wider move-aside → aggregate → settle section — the wrapper passes a suppressed `--lock-already-held` to the child (only when it actually took the lock), and the child then skips acquisition; a completed write closes-before-unlinks the lock (Windows-safe) and never removes a lock it did not create. `verdict.json.lock` is not `*.json`, so it never enters the attestation. Revert-proof: `t_aggregate_cli_refuses_while_verdict_lock_held` (a direct aggregate refuses and does not overwrite `verdict.json` while the lock is held — fails on `c87411f`, where it ignores the lock and writes) and `t_aggregate_cli_lock_already_held_skips_acquisition` (the `--lock-already-held` child writes under a held lock and does not remove it); the existing `t_mcp_fail_verdict` guards end-to-end that the real MCP-spawned child does not self-block on the wrapper's lock.
    - Correction from the CodeRabbit re-review (commit `c087f53`):
      - **The aggregate lock's re-entrancy signal is now unforgeable by a standalone caller** (CodeRabbit 🟠 Major `r3951923661`): fix-37 let the MCP-spawned `aggregate.py` child skip re-acquiring the lock its parent holds via a `--lock-already-held` flag — but `argparse.SUPPRESS` only *hides* an option, it still *accepts* it, so a direct `aggregate.py --lock-already-held` could bypass a held lock and then be rolled back by the MCP settle, re-opening the very hole fix-37 closed. The flag is removed. `mcp_server.h_aggregate` now mints a random token, writes its **SHA-256 hash** into the `0o600` `verdict.json.lock` it owns, and hands its child the **preimage** via the `AR_AGGREGATE_LOCK_TOKEN` environment variable (set only for that child's call and restored immediately after, so it never leaks to other subprocesses); `aggregate.py` skips the lock **only** when its env token hashes to the stored hash. A standalone invocation has no such token and — seeing only the hash in the owner-only lock file — cannot invert it to a matching preimage, so it can never bypass a held lock (it takes the lock and is refused, exit 3). An inherited-fd capability would be stronger still but is POSIX-only (`os.open(dir_fd=…)`/fd-passing) and would break the Windows MCP path, so the hash-token handshake is the cross-platform choice. Revert-proof: `t_aggregate_cli_parent_token_skips_lock` (a matching preimage writes under the held lock) and `t_aggregate_cli_unforgeable_token_no_public_bypass` (a wrong token cannot bypass → refused, `verdict.json` untouched; the retired `--lock-already-held` flag is no longer accepted), both failing on `c087f53`; `t_mcp_fail_verdict` still guards end-to-end that the real MCP-spawned child does not self-block. Lock semantics are documented in `references/schemas.md`.
    - Corrections from the Codex re-review (commit `adfa69f`) — three TOCTOU/confinement follow-ups on the lock and snapshot work:
      - **The direct `aggregate.py` lock now covers the artifact READS, not only the writes** (Codex P1 `r3952220744`): fix-37/38 acquired `verdict.json.lock` just before writing, but the verdict is COMPUTED from the run's gates/reports/attested artifacts BEFORE that point. A direct aggregate paused after reading stale artifacts could, after a concurrent aggregation committed a fresher verdict and released the lock, acquire the lock and overwrite it with the stale `out` — replacing a fresh `FAIL` with an earlier `PASS` whose attestation omits the newer artifacts (Codex reproduced the drift). The lock (and its unforgeable-token re-entrancy check) is now taken **before the first artifact read** and held through both writes, so the whole read→compute→write is one critical section and a concurrent aggregate refuses (`O_EXCL`) rather than interleaving. Revert-proof `t_aggregate_cli_lock_precedes_artifact_reads` (with the lock held and `run.json` unparseable, the fixed CLI refuses on the lock before reading; the base crashes parsing `run.json` first).
      - **The catalog snapshot fails closed when `os.O_NOFOLLOW` is unavailable** (Codex P2 `r3952220749`): `_snapshot_confined_catalog` opened the resolved leaf with `getattr(os, "O_NOFOLLOW", 0)`, which silently ORs in `0` on a platform without it (Windows) — and `O_NOFOLLOW` is **load-bearing**, not defense-in-depth: `resolve()` canonicalizes the path but does not pin the object across the reopen, so a concurrent swap of the resolved leaf to an out-of-tree symlink is caught only by `O_NOFOLLOW`. It now **raises** when `os.O_NOFOLLOW` is absent, exactly as `_read_policy_racesafe` already does (fix-35), rather than snapshotting an external catalog. Revert-proof `t_mcp_catalog_snapshot_fails_closed_without_o_nofollow` (with the attribute removed, the fixed snapshot raises; the base proceeds via `getattr(...,0)`).
      - **`ar_aggregate`'s settle refuses to promote a symlink-swapped `.prev` stash** (Codex P1 `r3952220753`): fix-21's guard rejects a symlinked `.prev` present at ENTRY, but after the wrapper itself creates the stash (`vf.replace(cand)`), a concurrent process with run-dir write access could replace `.prev` with a symlink before a rejected aggregation reaches the settle; `stash.is_file()` follows it and `os.replace` (renaming the link, not its target) would promote it to `verdict.json`, after which `ar_get_verdict` follows it to an out-of-run file (Codex reproduced reading an external secret). The settle now `lstat`s the stash (`is_symlink`) and **refuses to promote a symlinked stash**, and — belt-and-suspenders against a swap in the tiny lstat→rename window — never leaves `verdict.json` a symlink (removes it and surfaces the failure). Either way the restore fails closed (`verdict.json` absent → RECOVERY_PENDING), so no external target is ever read back as the verdict. Revert-proof `t_mcp_aggregate_refuses_symlink_swapped_prev_stash` (a `.prev` swapped to a symlink mid-run is not promoted; the base leaves `verdict.json` a symlink to the external file). The stronger owned-descriptor stash (which would also PRESERVE the prior under such a swap) is a robustness item for the E3-S2c concurrency hardening, where the run dir is no longer assumed single-writer; this change closes the confidentiality (arbitrary-read) core now.

## [0.2.0]

### Added
- **Experimental local Streamable-HTTP transport for `ar-mcp` (E3-S2a).** `AR_MCP_TRANSPORT=http` (or `--http`) starts a stdlib `http.server` MCP endpoint that reuses the same `serve_message()`/`handle()` dispatch as stdio: a **POST** carrying JSON-RPC returns `application/json` for a response or **202** for a notification; requests are `Origin`-validated (DNS-rebinding defense) and the listener binds `127.0.0.1` by default — a **non-loopback bind is refused** (there is no auth yet), so the endpoint cannot be exposed to the network from here. Non-POST methods → `405`, an oversized body → `413`, an unsupported `MCP-Protocol-Version` → `400`, and a rejected request closes its connection (no keep-alive desync). **No authentication or session yet** — it is localhost-only (enforced) and MUST NOT be exposed remotely until the auth sub-story (E3-S2c) lands; `stdio` stays the default. New env: `AR_MCP_HTTP_HOST` / `AR_MCP_HTTP_PORT` / `AR_MCP_HTTP_ORIGINS` / `AR_MCP_HTTP_MAX_BYTES`. Still **not** a command-execution surface.
- Multi-sample corroboration of high/critical findings (E4-S3): the opt-in `AR_HIGH_SAMPLES=N`
  (integer, default `1`; also the policy key `high_samples`) makes `panel.py run` re-run any role
  that raised a `high`/`critical` finding to `N` low-temperature samples and record a
  `corroboration: {samples, agreed, rate}` object on that finding — the share of samples whose own
  high/critical findings match it (same file + similar title via a `difflib` ratio). Only flagged
  roles are resampled, each sample is recorded under `panel/samples/` (with cost metered under
  `panel/meta/`, honoring the per-run cost cap — a resample that would cross the cap records a
  `corroboration`-phase `cost_abort.json` and BLOCKS rather than overspend), and `N=1` is a strict
  no-op (byte-identical reviewer artifacts). The agreement rate is **informational only**:
  `aggregate.py` alone still decides the verdict, so disagreement never overrides the gate. See
  `references/config.md`. Review hardening (CodeRabbit + Codex on #48): `high_samples` is validated
  at policy-load exactly as `panel.py run` resolves it (`int(str(value))`), so an integral-looking
  float (`3.0`, `1e1`) that `init` would accept can no longer be rejected later at `run`; a plain
  `run` resume no longer re-buys corroboration samples (only roles produced in that invocation are
  resampled, so recorded cost can't undercount real spend); the `corroboration` object is declared in
  the finding schema so an enriched report stays schema-valid; the resolved count + source are
  persisted to `sample_policy.json` (even at the default `1`) for the audit; a cost-cap abort during
  corroboration names every later flagged role it skipped; and the keyless prepare/ingest (MCP) path
  now surfaces that corroboration is not applied there instead of silently ignoring `high_samples`.
- Release hygiene: this changelog and a "cutting a release" ritual in `CONTRIBUTING.md`.
- Documentation drift guards: `tests/run_tests.py` now asserts the gate matrix in
  `references/gates.md` stays consistent with `MINIMUM_GATES` in `scripts/gate.py`, and that
  no hardcoded test-scenario counts are reintroduced into the README.
- Test-harness seam: `tests/mock_router.py` supports a swappable `response_provider` and a
  `reset()` helper, so future suites can inject per-scenario reviewer responses without
  forking the request handler.
- Model-capability profile: `scripts/_common.py` can resolve a per-model capability profile
  (catalog defaults merged with an optional `.adversarial-review.capabilities.yml` and an
  `AR_CAP_OVERRIDES` env override), laying the groundwork for capability-driven request
  building.
- Reviewer meta-evaluation corpus (E1-S1): a versioned, self-describing case format under
  `evals/corpus/<case-id>/` (`meta.json` / `context.md` / `expected.json`) with a stdlib
  validator (`evals/corpus_schema.py`) that reuses the panel's schema checker, seed cases
  across every defect category plus clean cases, an `evals/README.md`, and CI coverage. This
  is the groundwork for measuring the panel's true-positive / false-negative / false-positive
  rates (scoring lands in E1-S2).
- Reviewer meta-evaluation scoring (E1-S2): `evals/score.py` grades reviewer findings against the
  corpus ground truth. A finding matches a defect when it is in the same file **and** either its
  cited line is within a locator's range (±`line_tol`, default 3) **or** the finding's text names one
  of the defect's `root_cause_tags` — file-overlap is required even on the tag path, so a root-cause
  word in the wrong file is not a match (neither line-only nor tag-only is sufficient on its own).
  Scoring is severity-aware: a `must_detect` defect matched at/above its `severity_floor` is a true
  positive, a match below the floor is a **partial** (noticed but under-rated, not a detection), an
  unmatched one is a false negative; a `must_detect: false` defect is informational. A false positive
  is a finding matching no defect — any finding beyond `fp_budget` on a clean case, or an unmatched
  high/critical one beyond budget on a defect case (an extra low/medium is noise). `aggregate()` rolls
  per-case results up overall and per category/tier. Pure, stdlib-only, 3.9-safe, 100% branch-covered
  offline (`t_eval_score_*`); the offline harness that drives it is E1-S3 (below).
- Reviewer meta-evaluation offline harness (E1-S3): `evals/run.py --mode offline` drives the **real**
  panel pipeline (`panel.py assign` → `run` → ingest) over the corpus against the in-process mock
  router, serving each case's scripted reviewer findings — a new optional `scripts.offline` block in
  `expected.json` — through the E0-S1 `response_provider`, then scores the ingested findings with
  `evals/score.py`. It emits a dated `evals/report/<ts>.json` plus a human-readable `summary.md` with
  true-positive / partial / false-negative / false-positive metrics overall and per category and tier,
  plus a per-reviewer-role breakdown (findings emitted, and how many were true positives, partials, or
  unmatched — per-role FN/FP are not attributed, since which role should catch a given defect is not
  encoded). Because reviewer outputs are scripted, it measures harness correctness, not
  model quality (live calibration is E1-S4); a role omitted from a case's script is a deliberate miss
  and an extra finding is a false positive, so the seed corpus exercises every scoring outcome
  end-to-end. Deterministic (same corpus + scripts → byte-identical scored `result`; wall-clock
  stamped only outside it), stdlib-only, 3.9-safe, no network and no keys. A fast 2–3 case subset runs
  in the suite on every push; the full corpus runs as a separate CI `evals` job. This change also
  **restores the `t_eval_score_*` scorer tests** (100% branch coverage) that the E1-S2 (#44) merge
  dropped from `tests/run_tests.py` — `evals/score.py` had shipped to `main` untested — so the scorer
  the harness relies on is verified again.
- Reviewer meta-evaluation live calibration (E1-S4): `evals/run.py --mode live` runs the corpus
  through **real** model panels (`--reps N` per case) over the configured transport, scoring each
  panel's ingested findings with `evals/score.py`. Unlike offline mode it serves no scripts and starts
  no mock router — reviewers answer for real — so it measures model + panel quality, not just harness
  assembly. It emits a dated `evals/report/live-<ts>.json` + `live-<ts>.summary.md` with the detection
  rate per category and tier, a per-reviewer-role and per-model contribution breakdown (findings
  emitted / true positives / partials / unmatched, plus cost per model), the clean-case false-positive
  rate (`clean_fp_rate`), cost per case, and per-rep detail so single-run noise stays visible (per-role
  and per-model FN / detection-rate are not attributed — which role/model should catch a defect is not
  encoded); each case runs at its own tier (a SENSITIVE
  case gets the six-role panel). A cumulative USD budget (`--budget-usd`, default **$20**) caps the
  whole run: checked before each panel, and when reached the remaining `(case, rep)` units are recorded
  in `not_run` and the run stops — never a silent partial, overshoot bounded by the one in-flight
  panel; each individual panel still honours its own `AR_MAX_COST_USD` cap (E4-S2). Opt-in and never in
  CI: it needs a provider key resolved the same way the panel resolves it (`OPENROUTER_API_KEY` /
  `AR_API_KEY` / an existing `AR_KEY_FILE`; required even behind an `AR_BASE_URL` proxy) and exits
  rather than spend a run on no-op panels without one. Live results are non-deterministic by nature (the per-rep raw detail
  keeps the aggregate auditable); the harness code itself is exercised offline against the mock router
  with no network and no keys. Regression thresholds seeded from the first live report are E1-S5.
- Reviewer meta-evaluation regression thresholds (E1-S5): `evals/thresholds.json` records **descriptive**
  floors and `evals/thresholds.py` enforces them. `thresholds.py check` runs the offline harness and fails
  (exit 1) when the overall or per-category detection rate drops below, or the false-positive count rises
  above, the committed floor, so a change to `score.py`, the corpus scripts, or the panel wiring that
  quietly weakens detection is caught in CI (the `evals` job now runs it on 3.9 and 3.12). Because offline
  serves scripted findings this guards harness/scorer/dispatch correctness, not model quality; the floors
  are seeded from the current deterministic offline run (overall detection >= 0.5 and <= 1 FP; security and
  correctness >= 1.0) and are updated deliberately, never invented (mirrors the mutation-threshold rule in
  `references/gates.md`). `thresholds.py compare` is the **model-degraded alarm**: it diffs two live
  calibration reports and flags any overall/per-category detection drop past `live.max_detection_drop`
  (default 20%) plus any model whose true-positive contribution fell — a candidate for pin removal /
  substitution — with a runbook in `evals/README.md`. The baseline live report is committed after the first
  live calibration (the E1-S4 $20 run). Stdlib-only, 3.9-safe.
- Distribution: a `release` workflow publishes to PyPI on a `v*` tag via **Trusted Publishing**
  (OIDC, no stored token); an `action-selftest` workflow exercises the composite action keyless
  and asserts the honest BLOCKED verdict; a GitLab CI template (`examples/.gitlab-ci.yml`); and a
  `Changelog` project URL.
- CI-integration guide + Marketplace steps (E2-S3): a new `docs/ci-integration.md` presents **both**
  the GitHub Action and the GitLab template together — the Action's real inputs (`risk`,
  `dev-providers`, `gates`, `fail-on`, `diff-ref`, `product`, `openrouter-api-key`) and outputs
  (`verdict`, `exit-code`), the keyless→BLOCKED path, the verdict→exit-code (`0`/`1`/`2`) contract,
  and an Action-input↔GitLab-variable mapping. It also documents the remaining **manual** GitHub
  Marketplace publishing steps a maintainer performs by hand (publish from the repo's Releases page,
  choose categories, and adopt a moving `v1` major tag; branding already lives in `action.yml`),
  since Marketplace listing cannot be automated from this repo. Referenced from `README.md`,
  `docs/using-on-your-platform.md`, and the docs-site footer. A `t_ci_docs_and_gitlab_mirror_action`
  guard test asserts the guide names every real `action.yml` input and output (no invented inputs),
  proves `aggregate.py` is ar-panel's terminal command (so the job exit code is the verdict), and
  locks in the secrets-transmission guard and the full-floor starter workflow below.
  Review hardening (CodeRabbit + Codex on #49): the composite **`action.yml` now gates diff
  transmission to the reviewer panel on a passing `secrets` gate** — mirroring the GitLab job, so a
  correctly-configured secrets scan is required before the diff is transmitted, and every
  gate/panel/aggregate step is
  pinned with `--run` to the exact run directory `panel.py init` created, so a repository-committed
  `.adversarial-review/run-*` in the checkout cannot forge the secrets gate or hijack the pipeline;
  the documented starter workflow
  configures the whole NORMAL floor (build/unit/secrets/deps/sast) with explicit `risk`/`dev-providers`
  so it is a complete, safe copy; and the guide now distinguishes the computed **verdict / `exit-code`**
  from the **job pass/fail status** that `fail-on` (GitHub) / `allow_failure` (GitLab) derive from it,
  noting that a failed deterministic gate takes precedence over BLOCKED.
- Reviewer robustness & cost control (E4): `build_request` is now capability-driven — a model whose
  profile forbids `temperature` is sent none, `max_tokens` is floored at the profile's
  `max_tokens_floor`, and a mandatory-reasoning model receives a `reasoning` budget
  (`AR_REASONING_EFFORT`, default `high`); a profile's `structured_outputs` flag overrides the
  catalog's in either direction (so a wrong catalog can't force an unsupported `response_format`);
  and a model with an all-default profile gets the byte-identical request it did before — the
  pre-E4 key order (`temperature` before `max_tokens`) is preserved, not just the values. A per-run
  cost ceiling (`AR_MAX_COST_USD`, or policy `max_cost_usd`, default `$20`) is enforced as a
  pre-call gate across **every** paid phase — panel, rebuttal, and concurrence — aborting the
  remaining calls once reached (each phase records its own skipped work), recording a BLOCKED cost
  reason, and surfacing the run's total. The cap the run records in `cost_policy.json` is
  authoritative for the whole run: rebuttal and concurrence reuse it rather than re-resolving live
  settings, so changing `AR_MAX_COST_USD` mid-run can neither disable nor raise it. Also surfaced:
  `cost_usd` plus the enforced `cost_cap_usd`/`cost_cap_source` on the verdict; a verbose model can
  raise the bill but never buy a silent partial PASS. Cost accounting is hardened end to end: a
  malformed-JSON retry (a second billed call) is fully counted, the MCP-ingest path's nested
  `usage.cost` is read, non-finite/negative per-reviewer costs are metered as `$0`, and a
  non-finite or negative cap is rejected at policy load and at resolution rather than silently
  disabling the guard. The new `coverage.cost_usd` / `cost_aborted` / `cost_cap_usd` /
  `cost_cap_source` fields and the cost-triggered BLOCKED reason are documented in
  `references/schemas.md`; being a pre-call gate (not a reservation), the recorded total can
  overshoot the cap by up to the in-flight reviewer's cost, as `references/config.md` now states.

### Changed
- MCP server transport seam (E3-S1): the stdio framing in `scripts/mcp_server.py` is extracted
  into a `StdioTransport` class around a transport-agnostic `serve_message()` core, leaving the
  `handle()` dispatch untouched. Framing is byte-identical for well-formed and ordinarily-malformed
  messages (`-32700` parse errors, `-32603` handler-crash containment, notification suppression);
  the extracted parse guard catches every malformed-input failure `json.loads` can raise — a
  `JSONDecodeError`, a `RecursionError` from pathologically nested input, and a `UnicodeDecodeError`
  from bad bytes — and frames each as `-32700` instead of letting it escape and kill the loop
  (closing a pre-existing gap), so the seam's "a single malformed message never kills the transport"
  guarantee holds for the stdio loop and for the byte-oriented Streamable-HTTP transport (E3-S2) that
  will reuse the exact dispatch and error semantics. Stdlib-only, 3.9-safe.

## [0.1.0]

### Added
- Initial public release: deterministic gate recorder (`gate.py`), independent multi-model
  reviewer panel (`panel.py`), machine-computed verdict + attestation (`aggregate.py`), and a
  dual-era stdio MCP server (`mcp_server.py`). Zero runtime dependencies, Python 3.9+.

[Unreleased]: https://github.com/SathiaAI/adversarial-review/compare/v0.2.0...HEAD
[0.2.0]: https://github.com/SathiaAI/adversarial-review/releases/tag/v0.2.0
[0.1.0]: https://github.com/SathiaAI/adversarial-review/releases/tag/v0.1.0

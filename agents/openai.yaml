interface:
  display_name: "Adversarial Review"
  short_description: "Multi-model adversarial review with a deterministic release verdict"
  default_prompt: "Use $adversarial-review to independently review and verify this change before release."
